[package]
name = "lau-ensign"
version = "0.1.1"
edition = "2021"
description = "THE ensign system — small models that wake up on-call, orient from baton and room state, and keep rooms fine-tuned at yellow alert"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-ensign"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
